package model;

public class Admin extends User {

    public Admin(int id, String userName, String contact) {
        super(id, userName, contact);
    }

    // Additional attributes or methods specific to Admin
}
